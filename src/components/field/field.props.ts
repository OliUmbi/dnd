import {ReactNode} from "react";

export interface FieldProps {
  children: ReactNode,
  cxl: number,
  cl: number,
  cm: number,
  cs: number,
  cxs: number,
  rxl: number,
  rl: number,
  rm: number,
  rs: number,
  rxs: number,
}
